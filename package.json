{
  "name": "blueming",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "vercel-build": "mkdir -p public && cp index.html public/index.html"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
